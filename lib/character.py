import lib.constants as c

class Character():
    def __init__(self):
        self.spriteH = 50
        self.spriteW = 50
        self.sprite = (c.SURFWIDTH / 2, c.SURFHEIGHT / 2, c.TILESIZE, c.TILESIZE)

